package lpm.project.lpm_v0.repositories;

import lpm.project.lpm_v0.entities.BonDeReception;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BinDeReceptionRepository extends JpaRepository<BonDeReception,Long> {
}
